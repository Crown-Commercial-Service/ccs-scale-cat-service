spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://idp.example.com/issuer
          # More config necessary if no Provider Configuration / Authorization Metadata endpoint provided