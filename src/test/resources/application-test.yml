# Core Spring config
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://dummy.example.com

# Custom config items
config:
  flags:
    devMode: true        
  retry:
    maxAttempts: 3
  external:
    agreementsService:
      baseUrl: https://dummy.example.com